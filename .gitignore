# 依赖：只有重新打包 CodeMirror 时才需要 npm install
node_modules/

# vendor/cm6.js 是**故意签入**的生成物，不要忽略它 ——
# 日常改代码不需要装依赖，双击 index.html 就能跑，靠的就是它在仓库里。
# 见 README「构建」一节。

# 编辑器 / 系统留下的杂物
.DS_Store
Thumbs.db
*.swp

# 跑无头的工具：变异批次和两个生成器。**有意不进仓库**（2026-09-22 用户：
# 「你写的那些跑无头的tools不要跟踪，只传和项目有关的」）—— 它们只在这台机器上
# 跑流程用，不是站点的一部分。
#
# ⚠ 代价是仓库里留下指不到东西的路径引用。**全是注释和文档，不影响任何断言**，
#   所以不用去"修"；但改这几个文件时别以为文件丢了。完整清单（2026-09-22 核过）：
#   mutate.js / mutate-check-clean.js ——
#     - docs/design-notes.md :185 :433 :488 :667 :697 :700 :785 :793 :1146（含工具表那行）
#     - package.json :12-13  "mutate": "node tools/mutate.js"，裸 clone 上跑会失败
#     - js/editor/codex-panel.js :50 :69 :475（引用 M14/M15/M20 的期望文案）
#     - tools/check-boot.js :1796（M14~M17、M20~M23）、:2635（N1~N9）
#   gen-module-docs.mjs ——
#     - docs/design-notes.md :549 :576 :1675
#     - data/module-docs.js :3（**产物且已签入**，但生成它的脚本没有，
#       所以别人拿不到重跑它的办法）、tools/check.js :165
#   gen-key-schema.mjs —— 没有任何地方引用它，删掉也不留痕
tools/mutate.js
tools/mutate-check-clean.js
tools/gen-key-schema.mjs
tools/gen-module-docs.mjs
