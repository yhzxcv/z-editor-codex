# Cloudflare Pages 响应头（放在输出目录根部，CF 会自己吃掉这个文件、不对外提供）
# 语法：第一行是路径匹配，下面缩进的是响应头。

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer
  Cache-Control: public, max-age=0, must-revalidate

# 为什么全站都不许浏览器长时间缓存：
#
# 站点没有构建步骤、文件名也不带内容哈希，所以「缓存失效」只能靠部署。
# 但部署清的是 Cloudflare 边缘节点的缓存，管不到**访客浏览器**里已经存下的副本。
# 曾经这里给 /css/* /js/* /data/* 配过 max-age=3600，结果是：
# index.html 走 CF 默认的「每次回源校验」所以是新的，而 js/css 一小时之内不回源所以是旧的——
# 新按钮画得出来但点了没反应、新样式不出现。这类「半个新版本」最难排查。
#
# max-age=0 + must-revalidate = 每次都回源校验：文件没变 CF 回 304（全站合计才 240KB），
# 变了就立刻生效。以后改 css/js 直接推就行，不需要再动这个文件。
